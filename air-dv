#!/bin/sh
# Run AIR-DV from a repository checkout without configuring PYTHONPATH manually.

project_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHONPATH="$project_root/src${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONPATH
exec "$project_root/.venv/bin/python" -m air_dv analyse "$@"
